import {StyleSheet} from 'react-native';

const GlobalStyles = StyleSheet.create({
  textBold: {
    fontSize: 14,
    color: 'white',
    fontWeight: 'bold',
  },
  textSemiBold: {
    fontSize: 14,
    color: 'white',
    fontWeight: '600',
  },
  textNormal: {
    fontSize: 14,
    color: 'white',
    fontWeight: '400',
  },
});

export default GlobalStyles;
