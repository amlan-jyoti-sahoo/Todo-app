import {StyleSheet} from 'react-native';

const GlobalStyles = StyleSheet.create({
  textBold: {
    fontSize: 14,
    color: 'white',
    fontWeight: 'bold',
  },
  textNormal: {
    fontSize: 14,
    color: 'white',
    fontWeight: '400',
  },
});

export default GlobalStyles;
