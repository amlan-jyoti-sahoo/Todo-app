import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

const RepeatWidget = () => {
  return (
    <View>
      <Text>RepeatWidget</Text>
    </View>
  );
};

export default RepeatWidget;

const styles = StyleSheet.create({});
